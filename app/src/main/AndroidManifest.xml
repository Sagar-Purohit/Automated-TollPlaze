<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.tollapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".test_demo"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OptionsForRegistration" />
        <activity android:name=".Viewroot" />
        <activity android:name=".Tooll" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".Onlinepaymentscreen" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".EnterOtp" />
        <activity android:name=".Changepassword"></activity>
    </application>

</manifest>